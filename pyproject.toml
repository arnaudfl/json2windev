[project]
name = "json2windev"
version = "0.1.0"
description = "JSON to WinDev structure generator"
requires-python = ">=3.11"
dependencies = ["pyyaml"]

[project.scripts]
json2windev = "json2windev.app.cli:main"
